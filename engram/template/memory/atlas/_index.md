<!-- Area index (Map of Content) — format spec, not a real index.
     Created by /mem-init or /mem-sync when the master Atlas TOC would push MEMORY.md
     past its 120-line budget (roughly >20 cards). Filename: INDEX-<area>.md, e.g.
     atlas/INDEX-backend.md. The master Atlas table then holds ONE row per area:

       | [[INDEX-backend]] | Backend: auth, api, db, queue (12 cards) | ✓ all fresh |

     and sessions climb master → area index → card. Aggregate freshness for the master
     row: "✓ all fresh" when every member card is fresh, else "⚠ N stale".

     NO frontmatter — index files are TOCs, not cards; the staleness machinery must
     ignore them. Fully regenerated by /mem-sync from the member cards on disk (the
     one-line area summary survives regeneration; table rows do not). -->

# INDEX-example-area

One-line summary of what this area of the codebase covers.

| Card | What it covers | Freshness |
|------|----------------|-----------|
| [[example-module]] | One-line scope | ✓ 2026-01-01 |
